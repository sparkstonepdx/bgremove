{
  "name": "bgremove-wasm",
  "private": true,
  "description": "A background remover that runs in the browser tab. scripts/build.mjs copies the page, the runtime, and one model into a static site.",
  "dependencies": {
    "coi-serviceworker": "^0.1.7",
    "onnxruntime-web": "1.23.0"
  },
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "serve site",
    "check": "pnpm --filter harness check"
  },
  "devDependencies": {
    "serve": "14.2.6"
  },
  "workspaces": [
    "harness"
  ]
}
